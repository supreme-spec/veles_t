import { travelTipsMetadata } from './metadata';

export const metadata = travelTipsMetadata;

export default function TravelTipsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
